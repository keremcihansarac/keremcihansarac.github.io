// Repo names to pin at the top of the Projects section, in this order.
// Everything else follows, sorted by last update (the API's order).
export const featuredRepos = ['keremcihansarac.github.io']
